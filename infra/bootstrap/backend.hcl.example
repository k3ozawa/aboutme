# Copy to backend.hcl, then run `mise run tf:bootstrap:init`.
bucket       = "k3ozawa-tf-backend"
key          = "aboutme/bootstrap/terraform.tfstate"
region       = "ap-northeast-1"
encrypt      = true
use_lockfile = true
